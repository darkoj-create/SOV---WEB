# SOV web v6.1.45s — Dashboard daily UX/header clean

Scope: `dashboard.html` only + one CSS asset.

Changes:
- Replaced text-only SOV pill in dashboard header with real `assets/sov-logo.png` logo.
- Logo now links to `index.html` / public site.
- Replaced misleading `Home -> dashboard.html` button with `Javni sajt -> index.html`.
- Kept `Odjava` separated as logout action.
- Removed repeated onboarding/meta text from dashboard hero/status/section heads.
- Status panel now keeps only role/access + account status.
- Removed decorative badges such as `čisto korisničko sučelje` and role-list badge.
- Added accessible focus states and responsive header sizing.

No SQL changes.
No DOC/DOCX included.
