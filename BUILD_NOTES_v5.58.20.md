# SOV web v5.58.20 — Karta full object detail

- `karta.html` now opens the same full object detail used by Arhivar/Baza via `sov_arhivar_get_object_detail`.
- Marker/list click first shows a loading state, then replaces the short map card with full human-readable sections.
- SQL/raw dump remains hidden from the user view.
- No new SQL required if the Arhivar workflow/detail RPC is already deployed.
- `sync-status.html` version updated to v5.58.20.
