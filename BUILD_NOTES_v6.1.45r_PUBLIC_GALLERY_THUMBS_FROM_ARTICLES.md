# SOV Web v6.1.45r — public gallery thumbs from articles

Frontend-only polish.

- Filled remaining empty archive/gallery thumbnail slots in `vijesti.html` using existing article thumbnails from `assets/legacy-wordpress-thumbs`.
- Added `assets/sov-gallery-thumbs-v6145r.js` to hydrate any future empty `.archive-img` / `.news-img` cards with article images instead of blank dark placeholders.
- No Supabase / SQL changes.
- No DOCX files added.
