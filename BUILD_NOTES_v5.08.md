# v5.08 — Safe SQL staging preview

Base: v4.99 stable.

- Speleo Baza ostaje na JSON-u.
- Dodana nova stranica `speleo-sql-safe.html`.
- Import ide samo u `speleo_objects_staging`.
- Nema prebacivanja živog weba na SQL.
- Nema brisanja, nema destructive SQL-a.
- Dupli JSON ID-jevi se preskaču prije upserta.
