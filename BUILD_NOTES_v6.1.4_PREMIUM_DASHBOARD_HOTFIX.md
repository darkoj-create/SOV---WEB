# SOV web v6.1.4 — Premium dashboard hotfix

## Scope

Frontend-only build focused on making the SOV Cloud dashboard look like a finished premium app rather than a dev/control panel.

## Changes

- Added `assets/sov-dashboard-premium-v614.css`.
- Restyled Dashboard hero, role cards, module cards, admin mini-actions, and Webmaster-only system tools.
- Rebuilt the visual behavior of the `Sustav` section so links are cards/buttons instead of cramped inline text.
- Updated dashboard visible build labels to v6.1.4.
- Updated `sync-status.html` cache/version expectation to v6.1.4 / 614.
- Updated `update.json`, `VERSION.txt`, `BUILD_VERSION.txt`, and `assets/sov-version.js`.

## SQL

No SQL required.

## Rollback

Rollback to v6.1.3 ZIP if needed. No database changes were made.
